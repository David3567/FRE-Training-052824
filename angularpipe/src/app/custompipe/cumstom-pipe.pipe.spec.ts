import { CumstomPipePipe } from './cumstom-pipe.pipe';

describe('CumstomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CumstomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
