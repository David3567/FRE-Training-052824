import { NewcolorDirective } from './newcolor.directive';

describe('NewcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new NewcolorDirective();
    expect(directive).toBeTruthy();
  });
});
